# UX Builder — Current Implementation Status

**Last updated:** 2026-04-14
**Plan reference:** `implementation-plan.md` (30 sections, 3,730 lines)
**Spec reference:** `implementation-reference-v1.md`

> **Sprint numbering correction (Sprint 1):** The earlier sprint table merged several plan sprints into one row. The plan actually defines 12 narrow sprints. The corrected table below mirrors the plan headings exactly. Sprint 1 in the plan is *storage layer only* — session manager and operation executor are separate sprints.

---

## Sprint Progress

| Sprint | Scope (per plan §) | Status |
|---|---|---|
| **0** | Project setup — scaffold, deps, type stubs, LLM pipeline proof-of-life (§6) | ✅ **Done** |
| **1** | Storage layer — full types, IStorage, FileStorage, MemoryStorage, WireframeManager (§7) | ✅ **Done** |
| **2** | Operation Executor — streaming, parsers, retry, token tracker, two-AI pattern (§8) | ✅ **Done** |
| 3 | Context Builder — phase 1 + phase 2 context assembly, summarizer (§9) | ⏳ Not started |
| 4 | Session Manager — coordinator, phase state machine, op router, dependency graph (§10) | ⏳ Not started |
| 5 | **Phase 1 Complete** — ops 1.0–1.3, prompts, API routes, **first usable demo** (§11) | ⏳ Not started |
| 6 | Phase 2 auto-generation chain — ops 2.1–2.5 (§12) | ⏳ Not started |
| 7 | Drift detection + cascade engine — ops 2.6, 2.7 (§13) | ⏳ Not started |
| 8 | Wireframe viewer + test harness — ops 2.8, 2.9 (§14) | ⏳ Not started |
| 9 | Rollback + checkpoint system (§15) | ⏳ Not started |
| 10 | Polish, error UX, performance (§16) | ⏳ Not started |
| 11 | E2E tests + verification pass (§17) | ⏳ Not started |

---

## Sprint 0 — Detailed Record

### Goal
Prove the end-to-end LLM pipeline works from a Next.js route handler to a live model, against **both** a hosted provider (Groq) and a local provider (Ollama), using `@mariozechner/pi-ai` as the unified client layer. No business logic.

### Files created

#### Next.js scaffold
Generated by `npx create-next-app@latest app --ts --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --no-turbopack`, producing `app/` with Next 16.2.3, React 19, TypeScript, Tailwind 4.

#### Dependencies added
- **Runtime:** `@mariozechner/pi-ai@0.67.1`, `zustand`, `yaml`, `react-markdown`, `remark-gfm`, `uuid`
- **Dev:** `@types/uuid`, `vitest`

#### Files authored by us

| Path | Purpose |
|---|---|
| `app/tsconfig.json` | Added path aliases `@core/*`, `@components/*`, `@stores/*`, `@lib/*` |
| `app/src/core/llm/providers.ts` | `resolveModel(role)` + `describeActiveConfig()` — env-driven provider switching |
| `app/src/core/types/domain.ts` | (deleted in Sprint 1) Placeholder `SessionId`/`Session` types — replaced by full per-domain files |
| `app/src/core/types/llm.ts` | `LLMMessage`, `LLMUsage`, `ParsedOutput`, `ParseError`, `ParseResult` (expanded in Sprint 1) |
| `app/src/core/types/index.ts` | Barrel export |
| `app/src/app/api/health/route.ts` | `GET /api/health` returning live LLM config |
| `app/src/app/api/test-llm/route.ts` | `GET /api/test-llm` — SSE stream proof-of-life route |
| `app/.env.example` | Documents both Groq and Ollama env var options |
| `app/.env.local` | Active config — currently set to Ollama + `qwen3.5:4b` |

#### Skeleton directories (empty with `.gitkeep`)
`src/core/storage/`, `src/core/operation-executor/`, `src/core/session-manager/`, `src/core/prompts/`, `src/stores/`, `src/components/`, `src/lib/`

### Provider abstraction contract

Two provider paths wired end-to-end through `src/core/llm/providers.ts`:

**Groq path** — uses pi-ai's built-in Groq model registry.
- Env: `LLM_PROVIDER=groq`, `GROQ_API_KEY=<key>`
- Default fast model: `llama-3.1-8b-instant`
- Default reasoning model: `llama-3.3-70b-versatile`
- Override with `LLM_MODEL_FAST` / `LLM_MODEL_REASONING`

**Ollama path** — builds a custom `openai-completions` model against Ollama's OpenAI-compat endpoint.
- Env: `LLM_PROVIDER=ollama`, `OLLAMA_BASE_URL=http://localhost:11434/v1`, `OLLAMA_MODEL=qwen3.5:4b`
- Notable: pi-ai's openai-completions transport requires a non-empty `apiKey` argument even when the backend doesn't authenticate — the route passes the literal string `"ollama"` for the local path. This is safe because Ollama ignores it.

The `RoleName` type (`"fast" | "reasoning"`) is the abstraction the rest of the system will use when requesting models. Operations in later sprints ask for a role, not a provider.

---

## Testing Record — Sprint 0

### What was tested

| Check | Command | Result | Evidence |
|---|---|---|---|
| TypeScript compilation | `npx tsc --noEmit` | ✅ 0 errors | Silent exit |
| Next dev server boot | `npm run dev` | ✅ Ready in 3.8s | `▲ Next.js 16.2.3 (Turbopack)` |
| Health route | `curl http://localhost:3000/api/health` | ✅ | `{"status":"ok","llm":{"provider":"ollama","fast":"qwen3.5:4b","reasoning":"qwen3.5:4b","baseUrl":"http://localhost:11434/v1"}}` |
| Ollama live stream | `curl -N "http://localhost:3000/api/test-llm?role=fast&prompt=Say+exactly:+hi"` | ✅ | SSE: `meta` → `text_delta:"hi"` → `done:stop` → `final{input:30, output:197}` → `[DONE]` |
| Ollama reachability | `curl http://localhost:11434/api/tags` | ✅ | `qwen3.5:4b`, `qwen3:4b`, `embeddinggemma:latest` present |
| `.env.local` hot-reload | Edited while server running | ✅ | Next logged `Reload env: .env.local` |
| SSE error surface | Initial run without dummy key | ✅ (negative case) | Clear error event: `"OpenAI API key is required..."` — confirmed that pi-ai errors surface cleanly through our SSE dispatch |

### What was NOT tested (blind spots)

These are deliberate omissions for Sprint 0 — each is flagged for a later sprint.

| Blind spot | Why not | Planned coverage |
|---|---|---|
| **Groq live API call** | No `GROQ_API_KEY` on machine. The Groq code path is wired and its model IDs resolved via `getModel('groq', …)` in pi-ai's built-in registry, but nothing has been fired against the live Groq endpoint. | Sprint 1 — will run both paths through the Operation Executor once the first real op lands. User needs to drop a key into `.env.local` first. |
| **`role=reasoning`** on live model | Only `role=fast` was exercised end-to-end. Both roles resolve through the same codepath when provider=ollama (both point to `OLLAMA_MODEL`), so this is cosmetic. | Sprint 1 — will be exercised by ops 1.1 Call A. |
| **Concurrent requests** | Single-request testing only. No verification that SSE streams don't step on each other, no backpressure check. | Sprint 3 (UI) and Sprint 6 (batch ops) will stress this. |
| **Request cancellation** | `AbortSignal` plumbing not wired into the test-llm route. pi-ai supports `signal` via `StreamOptions.signal`. | Sprint 3 — chat UI will wire this up. |
| **Token usage accuracy** | Usage numbers are reported but not validated against a known-good baseline. `{input:30, output:197}` for a 9-word prompt on qwen3.5:4b is plausible but unverified. | Sprint 1 — Operation Executor's `TokenTracker` will have unit tests. |
| **Cost calculation** | Ollama returns zero cost (correct — free). Groq costs never computed because Groq never called. | Sprint 1 — tested once Groq path is live. |
| **pi-ai model cache** | pi-ai may cache model definitions globally. No check that `resolveModel` returns fresh results when env vars change at runtime. | Low priority — env vars don't change mid-process in normal use. |
| **Error classification** | The SSE route surfaces errors as strings. No distinction yet between transient (retry), parse-failure, and fatal. | Sprint 1 — Operation Executor's 3-level error handling (Section 20 of plan). |
| **Long-output streaming** | Only tested with a ~2-word response. Nothing that stress-tests sustained streams or the ~8K max_tokens ceiling. | Sprint 6 (batch ops) will naturally exercise this. |
| **Windows path handling in routes** | None of the routes touch the filesystem yet, so Windows path quirks have not been exercised. | Sprint 1 — file storage layer. |
| **Tailwind v4 + React 19 quirks** | No UI rendered yet — no style bugs possible at this stage. | Sprint 3 (chat UI). |
| **Security headers / CSP** | No iframe or wireframe surfaces yet. The test-llm route has no special headers beyond SSE basics. | Sprint 8 (wireframe viewer). |
| **pi-ai version pin** | Installed as `latest`, resolved to 0.67.1. Not locked in `package.json` with a caret — actually, npm wrote `^0.67.1`. Breaking changes in pi-ai could bite us. | Check `package.json` lock state before Sprint 1; pin exactly if unstable. |

### Known quirks discovered during Sprint 0

1. **pi-ai error events carry an `AssistantMessage` in `event.error`**, not a string. The real message lives at `event.error.errorMessage`. Our SSE dispatch now reads that field.
2. **User messages require `timestamp: number`** in pi-ai's `Context.messages` — not just `{role, content}`. Missed on first pass, caught by `tsc`.
3. **pi-ai's openai-completions transport rejects an empty API key** even for a custom baseUrl that doesn't authenticate. Workaround: pass `"ollama"` as a dummy for local models.
4. **`qwen3.5:4b`** is the model tag locally installed. `qwen3:4b` is also present — swap via `OLLAMA_MODEL` env var.

---

## Sprint 1 — Detailed Record

### Goal
Working persistence layer with full CRUD for every data model from the spec. Two interchangeable backends (file + memory) behind a single `IStorage` interface, both passing the same test suite. No business logic, no LLM calls.

### Files created

#### Type definitions (full backfill, replacing Sprint 0 stubs)

| Path | Contents |
|---|---|
| `app/src/core/types/session.ts` | `Session`, `PhaseId`, `PhaseStatus`, `SessionStatus`, `PhaseState` |
| `app/src/core/types/documents.ts` | `Document`, `DocumentType`, `DocumentStatus` |
| `app/src/core/types/artifacts.ts` | `Artifact`, `ArtifactType`, `ArtifactStatus` |
| `app/src/core/types/operations.ts` | `OperationId` (37-member union covering all ops 1.0–2.10), `OperationStatus`, `OperationProgress`, `BatchItem` |
| `app/src/core/types/chat.ts` | `ChatMessage`, `MessageRole`, `MessageType`, `MessageMetadata` |
| `app/src/core/types/checkpoints.ts` | `Checkpoint`, `DocumentSnapshot`, `ArtifactSnapshot`, `CascadeSnapshot`, `CascadeSnapshotStatus` |
| `app/src/core/types/tests.ts` | `TestRunResult`, `IndividualTestResult`, `TestStatus` |
| `app/src/core/types/llm.ts` | (expanded) `OperationDefinition<T>`, `OperationResult<T>`, `ExpectedOutputFormat`, plus the existing `LLMMessage`/`ParsedOutput`/`ParseError`/`ParseResult` |
| `app/src/core/types/config.ts` | `AppConfig`, `ModelConfig` |
| `app/src/core/types/index.ts` | (updated) Barrel exporting all of the above |
| `app/src/core/types/domain.ts` | **Deleted** (was the Sprint 0 placeholder) |

#### Storage layer

| Path | Contents |
|---|---|
| `app/src/core/storage/interface.ts` | `IStorage` (29 methods covering all 11 collections) + `ConversationSummary`, `PendingMessage`, `PendingMessageStatus` |
| `app/src/core/storage/file-storage.ts` | `FileStorage` — JSON-per-collection per session, atomic `.tmp` + `rename` writes, document versioning with active-flag flip |
| `app/src/core/storage/memory-storage.ts` | `MemoryStorage` — `Map<sessionId, SessionBucket>`, identical semantics, JSON deep-clone on read/write to prevent caller mutation |
| `app/src/core/storage/index.ts` | `getStorage(type, dataDir)` singleton factory + `resetStorage()` for tests + re-exports |
| `app/src/core/wireframe/wireframe-manager.ts` | `WireframeManager` — read/write/list/delete files under `data/sessions/[id]/wireframe/`. Atomic writes. `getContentType()` handles html/htm/js/mjs/css/json/svg + fallback |

#### Lib utilities

| Path | Contents |
|---|---|
| `app/src/lib/constants.ts` | `DEFAULT_TIMEOUT_MS`, `DEFAULT_MAX_RETRIES`, `DEFAULT_BATCH_CONCURRENCY`, `CHAT_HISTORY_TOKEN_BUDGET`, `RECENT_MESSAGE_PAIRS_TO_KEEP`, `STALE_TEST_CHECK_ENABLED` |
| `app/src/lib/utils.ts` | `generateId()` (uuid v4), `now()` (ISO 8601), `estimateTokens()` (chars/4 heuristic) |

#### Test infrastructure

| Path | Contents |
|---|---|
| `app/vitest.config.ts` | Vitest config with manual path-alias resolution mirroring tsconfig (vitest doesn't read tsconfig paths natively) |
| `app/src/core/storage/__tests__/storage.test.ts` | Single parameterized suite: 28 cases × 2 backends = **56 storage tests**. Plus 1 file-only persistence test = **57** |
| `app/src/core/wireframe/__tests__/wireframe-manager.test.ts` | **7 wireframe manager tests** |
| `app/package.json` | Added `test` and `test:watch` scripts |
| `app/.gitignore` | Added `/data/` to exclude file storage from git |

### Key design decisions made during Sprint 1

1. **Storage signature: `update*` methods require `sessionId` in the updates payload.** The plan's interface shows `updateDocument(id, updates)` with no session hint, but documents are partitioned by session on disk. Rather than scanning every session directory to find a doc by id, I made `sessionId` mandatory in `updates`. This adds one field at every call site but eliminates an O(N) lookup per write. **Trade-off accepted.** Tested explicitly — `updateDocument({ content: "..." })` without `sessionId` throws a clear error.

2. **MemoryStorage clones aggressively.** Every read and every write JSON-round-trips the value. Slower than reference-passing but matches FileStorage's behavior exactly: a caller can never mutate stored state by holding onto a returned object. Sprint 1's "isolation" test set explicitly verifies this.

3. **File storage: one JSON file per collection per session, not one file per record.** Trade-off: rewriting the whole `messages.json` on every new message is O(N) in messages, but for sessions with <1000 messages this is negligible (sub-millisecond for typical sizes). Buys us trivial atomicity — one `rename` per write, no lock files, no per-record state to manage. The plan calls this out as a known limitation; SQLite is the migration path if it ever bites.

4. **`getStorage()` is a singleton, but tests bypass it.** The factory returns the same instance for repeat calls within a process, which is right for the app but wrong for tests. Tests construct `new FileStorage(...)` and `new MemoryStorage()` directly with their own temp dirs. `resetStorage()` exists as an escape hatch but tests don't currently need it.

5. **WireframeManager uses atomic writes too.** The plan's example used a non-atomic `writeFile`, but I used `tmp + rename` for consistency. Wireframe HTML files are small, the cost is negligible, and it prevents half-written files if the process crashes mid-generation.

6. **Vitest path aliases are mirrored manually**, not via `vite-tsconfig-paths`. One fewer dependency, one config file to maintain. Documented in `vitest.config.ts` that the list must stay in sync with `tsconfig.json`.

---

## Testing Record — Sprint 1

### What was tested

| Check | Command | Result | Evidence |
|---|---|---|---|
| TypeScript compilation | `npx tsc --noEmit` | ✅ 0 errors | Silent exit |
| Unit tests | `npm test` | ✅ **64/64 passing** | `Test Files 2 passed (2) / Tests 64 passed (64) / Duration 918ms` |
| Sprint 0 health regression | `curl /api/health` | ✅ unchanged | `{"status":"ok","llm":{"provider":"ollama",...}}` |
| Sprint 0 LLM regression | `curl /api/test-llm?role=fast&prompt=...` | ✅ unchanged | SSE stream → `text_delta:"ok"` → `done:stop` → `final{input:31,output:82}` |

### What the 64 tests actually cover

**Both FileStorage and MemoryStorage (28 cases each):**
- Sessions: create, get, get-missing→null, update + updatedAt bump, list sorted-desc, delete
- Phase state: upsert, get, upsert-overwrite
- Documents: createDocument bumps version + deactivates prior, multi-type independence, updateDocument requires sessionId, updateDocument modifies content + lastModifiedAt, deactivateDocuments
- Artifacts: lifecycle generating→active→inactive, getArtifactsBySession
- Chat: addMessage + ordered getMessagesByPhase, phase filtering, updateMessage stale flag
- Operation progress: upsert + get, upsert overwrite, getOperationsBySession
- Checkpoints: create + getByNumber + getLatest, snapshot roundtrip
- Conversation summaries: upsert + get
- Pending messages: create + list + update
- Test results: save + getLatest by `runAt`
- Cascade snapshots: create + getLatest + update status
- Isolation: cross-session invisibility, returned objects decoupled from internal state

**FileStorage only (1 case):**
- Persistence — write data, construct a fresh `FileStorage(sameDataDir)`, read everything back

**WireframeManager (7 cases):**
- write + read roundtrip
- listFiles returns written filenames
- listFiles returns `[]` for unknown session
- deleteFile removes the file
- deleteFile is idempotent for missing files
- getContentType maps extensions (html/htm/js/css/json/svg/fallback)
- atomic write — no `.tmp` lingers on success

### What was NOT tested (Sprint 1 blind spots)

| Blind spot | Why not | Planned coverage |
|---|---|---|
| **Concurrent writes to the same collection** | Tests are single-threaded. Two simultaneous `addMessage` calls to the same session could race on the read-modify-write cycle, with the second clobbering the first. | Sprint 4 (Session Manager) — needs an in-process write lock per session. Untested today; flagged as a known correctness gap. |
| **Crash mid-write** | We rely on `rename` atomicity, but never simulate a crash to verify a half-written `.tmp` doesn't pollute the active file. | Hard to test in unit tests; would need fault injection. Accept the OS guarantee for now. |
| **Filesystem permission errors** | We never run against a read-only directory or denied path. | Defer to integration tests — handled via thrown errors that bubble up to API routes. |
| **Very large records / file size limits** | All test fixtures are small. No verification that a 10MB document or 10K messages perform acceptably. | Sprint 6 (batch ops) and Sprint 10 (perf) will stress this. |
| **Disk-full scenarios** | Untested. `writeCollection` would throw on `ENOSPC`, but no test verifies graceful surface-up. | Defer. |
| **Unicode / surrogate pair filenames** | `WireframeManager.getContentType` is case-insensitive but never tested with non-ASCII. | Low priority — wireframe files are LLM-generated and follow conservative naming. |
| **Cross-session checkpoint id collisions** | Checkpoints scoped per session, but if two sessions create checkpoints with the same id, no test catches the bleed. | Each checkpoint id is a UUID v4 — collision probability is astronomical. Not worth a test. |
| **JSON corruption recovery** | If `messages.json` becomes invalid JSON for any reason, `readCollection` throws. No fallback / quarantine logic. | Sprint 10 (error UX) — corrupt-storage recovery should land then. Currently a hard failure. |
| **Path-traversal in filenames** | `WireframeManager.writeFile(sessionId, "../../etc/passwd", ...)` would write outside the session dir. | **Sprint 8** — wireframe filenames come from LLM output and must be sanitized in the wireframe-serving route handler. **Tracked as a security gap.** |
| **Document `id` collision across sessions** | Possible if caller reuses ids. We don't enforce per-session uniqueness on create. | Caller responsibility — documented in `IStorage` contract. Not tested. |

### Known quirks discovered during Sprint 1

1. **Vitest 4 is happy with manual aliases** — no plugin needed once you mirror tsconfig paths in `vitest.config.ts > resolve.alias`. Caught early; documented in the config file.
2. **`updateDocument(id, updates)` without `sessionId`** is now a runtime error. The plan implied it would just work; the trade-off is documented in the design decisions above.
3. **Test isolation matters for IDs** — fixtures use a counter + timestamp so parallel test cases never collide on `uid("session")`.
4. **Next 16's project includes a sentinel `AGENTS.md`** warning that "this is NOT the Next.js you know." Honored by reading bundled docs in `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md` before writing anything in Sprint 0; route handler API is standard so no surprises landed in Sprint 1.

---

## Sprint 2 — Detailed Record

### Goal
A working Operation Executor — the universal LLM-call layer every operation in the system will route through. It streams, parses structured outputs, retries on parse failure with corrective prompts, tracks tokens, and works equally against Groq (hosted) or Ollama (local) without any code changes.

### Files created

#### Operation executor module (`src/core/operation-executor/`)

| Path | Contents |
|---|---|
| `executor.ts` | `OperationExecutor` class. Resolves models via `resolveModel(role)`, builds pi-ai `Context` with timestamp injection, streams events, accumulates text + usage, runs the op's parser, retries on parse failure with `buildRetryMessages`, returns `OperationResult<T>`. Honors per-op timeout via internal `AbortController` merged with caller's signal. `streamCall` is `protected` so unit tests can subclass and stub it. |
| `parsers.ts` | `parseYAML`, `parseJSON`, `parseMarkdownSections`, `parsePlainText`, `extractGenerationContext`, `extractChangeContext`, `parseValidationResult`, `parseDriftResult`, `parseDiagnosisResult`. All pure functions returning `ParseResult<T>`. Shared `stripCodeFences` helper handles ` ```yaml `, ` ```json `, and bare ` ``` ` wrappings. |
| `retry.ts` | `buildRetryMessages` — appends the failed assistant turn + a corrective user turn that quotes the parser error and the op-specific retry hint. |
| `token-tracker.ts` | `TokenTracker` — in-memory per-session accumulator. Forwards pi-ai's reported cost rather than recomputing from a hardcoded price table (since pi-ai already knows). |
| `streaming.ts` | `SSEWriter` + `createSSEResponse`. Standardized event vocabulary (`chunk`, `document`, `phase`, `progress`, `test_results`, `drift`, `meta`, `error`, `complete`). Used by every streaming API route in later sprints. |
| `two-ai-pattern.ts` | `executeTwoAIPattern(executor, callA, callBFactory, contextExtractor)` — orchestrates the universal Two-AI flow used by ops 1.1, 1.2, 2.7a. Short-circuits Call B when Call A returns a clarifying question. |
| `index.ts` | Barrel export for everything in the module. |

#### Type system updates

| Path | Change |
|---|---|
| `app/src/core/types/llm.ts` | Added `RoleName = "fast" \| "reasoning"`. Added `role?: RoleName` to `OperationDefinition` so ops can declare their model class without hard-coding a provider. |
| `app/src/core/llm/providers.ts` | Re-exports `RoleName` from `@core/types`. Added `getApiKey(provider)` helper. **Critical fix:** added `compat: { thinkingFormat: "qwen-chat-template" }` to the Ollama model so pi-ai sends `chat_template_kwargs: { enable_thinking: false }` — without this, qwen3 family models burn 30+ seconds per call on internal reasoning. |

#### Tests added

| Path | Cases | Coverage |
|---|---|---|
| `src/core/operation-executor/__tests__/parsers.test.ts` | 36 | Every parser, happy + failure cases, code-fence handling, missing fields, clarifying-question detection, drift classifications, diagnosis types, validation PASS/FAIL with bullet lists |
| `src/core/operation-executor/__tests__/token-tracker.test.ts` | 6 | Empty start, accumulation, session isolation, `reset(sessionId)`, `reset()`, copy semantics on getRecords |
| `src/core/operation-executor/__tests__/two-ai-pattern.test.ts` | 4 | Happy path (Call A → extract → Call B), clarifying-question short-circuit, Call A failure, Call B failure |
| `src/core/operation-executor/__tests__/executor.unit.test.ts` | 8 | Stubbed-streamCall executor: success-on-first-hit, retry-then-succeed, retry message context contents, retries-exhausted, no-retry-when-retryPrompt-null, transport-error-no-retry, sessionId token recording, no-recording-without-sessionId |
| `src/core/operation-executor/__tests__/executor.integration.test.ts` | 4 | Real Ollama qwen3.5:4b — simple plain_text call, streaming chunks forwarded, retry-then-succeed against live model, token tracker accumulation across multiple live calls. Skip-gated via Ollama reachability + model presence check. |

### Key design decisions made during Sprint 2

1. **Adapted the plan's executor to pi-ai's actual API.** The plan's literal code referenced `claude-sonnet-4-6` defaults and an `@anthropic-ai/sdk`-shaped streaming loop. My implementation uses `resolveModel(role)` from `core/llm/providers` and pi-ai's `stream(model, context, options)` shape that I learned in Sprint 0.

2. **Cost is forwarded, not recomputed.** The plan's `TokenTracker` had a hardcoded Anthropic price table and a `calculateCost` method. pi-ai already returns `usage.cost.total` per call from its internal model registry (zero for Ollama, accurate for hosted providers), so the tracker just sums what pi-ai reports. Less code, no drift between our table and pi-ai's.

3. **Per-op timeout is enforced via `AbortController`, not `Promise.race`.** When an op declares `timeoutMs`, the executor sets a `setTimeout(abort, timeoutMs)` and merges the resulting signal with the caller's `signal` (if any) via a small `mergeSignals` helper. This properly cancels the underlying pi-ai stream rather than leaving an orphaned background request.

4. **`streamCall` is `protected`, not `private`.** Unit tests subclass `OperationExecutor` to stub the network layer with a deterministic fake. This is more maintainable than `vi.spyOn` because TypeScript will catch renames at compile time.

5. **Integration tests are skip-gated, not deleted.** `executor.integration.test.ts` checks Ollama reachability + model presence in `beforeAll`. If either is missing, every test in the file logs a skip warning and returns success. This means CI stays green on a machine without Ollama, but a developer with Ollama running gets real coverage.

6. **The "exhausts retries" case is a unit test, not an integration test.** Originally I had it in the integration suite. Two sequential LLM calls on slow local hardware blew past any reasonable per-op timeout even with the thinking-disabled flag, so the test couldn't tell the difference between "executor correctly exhausted retries" and "executor correctly tripped the abort timeout". The unit-test version with a stubbed `streamCall` is deterministic, fast (5ms), and proves the same logic.

7. **Disabling Qwen3 thinking mode is mandatory for usability.** Without `chat_template_kwargs: { enable_thinking: false }`, qwen3.5:4b spends 200+ tokens of internal chain-of-thought per call, taking 35+ seconds for a "say hi" prompt. With the flag, the model still emits some reasoning but completes in ~10s. This was discovered while debugging the integration test timeouts and is the kind of thing that wouldn't show up in unit tests at all.

8. **Two-AI pattern returns the raw `OperationResult` for both calls.** Callers can drill into `callAResult.tokenUsage` and `callBResult.tokenUsage` for billing display, even though the `TwoAIResult` summary already has the human-readable parts.

---

## Testing Record — Sprint 2

### What was tested

| Check | Command | Result |
|---|---|---|
| TypeScript compilation | `npx tsc --noEmit` | ✅ 0 errors |
| Non-integration test suite (Sprints 0/1/2) | `npx vitest run --exclude="**/*.integration.test.ts"` | ✅ **118/118** passing in 1.14s (6 files) |
| Live Ollama integration suite | `npx vitest run src/core/operation-executor/__tests__/executor.integration.test.ts` | ✅ **4/4** passing in 228s against `qwen3.5:4b` |
| Sprint 0 routes regression | `curl /api/health` | ✅ unchanged |

### Test count breakdown (cumulative)

- **Sprint 0:** 0 unit tests (proof-of-life routes only)
- **Sprint 1:** 64 tests (57 storage + 7 wireframe)
- **Sprint 2:** 58 tests (36 parsers + 6 token-tracker + 4 two-ai + 8 executor-unit + 4 executor-integration)
- **Total:** **122 tests** (118 unit + 4 integration)

### What was NOT tested (Sprint 2 blind spots)

| Blind spot | Why not | Planned coverage |
|---|---|---|
| **Groq live calls** | No `GROQ_API_KEY` on machine. The Groq path is wired and `getApiKey("groq")` reads from env, but nothing has hit real Groq yet. | Sprint 5 — when ops 1.0–1.3 land, you can drop a key in `.env.local` and run the full Phase 1 flow against Groq. |
| **Concurrent execute() calls on one TokenTracker** | Single-threaded tests only. Two simultaneous `execute()` calls writing to the same tracker would race on the internal `Map.set`. | Sprint 4 (Session Manager) — needs a per-session lock or atomic accumulator. Currently a known correctness gap if two SSE streams from the same session run concurrently. |
| **AbortSignal cancellation mid-stream** | `signal` is wired through `executor → pi-ai`, but I never verified that aborting mid-stream cleanly stops the upstream HTTP request. The timeout path works (the integration test's earlier failure mode showed pi-ai surfacing "Request was aborted" correctly), but I didn't write a test that aborts a *running* operation from the caller side. | Sprint 3 — chat UI will need cancellation. Will add a test there. |
| **Markdown sections with nested `##` inside a section** | The current parser splits on every `^## ` line. Nested headers (e.g. `## H2` inside a `## Outer` body) would prematurely end the outer section. | Real-world prompt design avoids nested H2 in document outputs, so this is unlikely to bite. Note in code if it does. |
| **Diagnosis parser with multi-line root_cause containing `confidence:` substrings** | The field-stop regex looks for `confidence:` at the start of a line. If the model puts "we have low confidence:" inside the root_cause body, the parser will truncate. | Defer — can be tightened if real outputs trigger it. |
| **Token tracker persistence** | Tracker is in-memory only. Restarting the dev server zeros all session totals. | Sprint 4 — Session Manager will own persistence; tracker may need a `flushTo(storage)` method. |
| **Cost accuracy for Groq** | pi-ai's Groq model registry has price data baked in, but I haven't sanity-checked it against current Groq pricing. | Defer — first real Groq call will give us a number to compare. |
| **`extractGenerationContext` with multiple context blocks** | The parser uses `indexOf`, which finds the first one. Multiple blocks would silently lose the rest. | Real prompts ask for exactly one block. Note in code if it ever appears. |
| **Streaming backpressure** | `SSEWriter.send` enqueues without checking the controller's queue. A slow consumer could blow up memory. | Sprint 3 — chat UI will be the first real consumer; will add backpressure handling there. |
| **Two-AI pattern with parser failures inside Call B** | Tested for "Call B failed" (executor returns failed status), but not specifically "Call B's parser failed but the executor retried and eventually succeeded". | Existing executor unit tests cover the retry path; the two-AI orchestrator just relays whatever execute() returns. Coverage is transitively complete. |

### Known quirks discovered during Sprint 2

1. **Qwen3 thinking mode is on by default.** Without `chat_template_kwargs: { enable_thinking: false }`, qwen3.5:4b takes 35+ seconds for a 2-word response. The `compat.thinkingFormat: "qwen-chat-template"` flag in providers.ts is critical for usability, not optional.
2. **pi-ai's `Usage` interface requires `totalTokens` field** (not just `input` + `output`). Not documented anywhere obvious — caught by TS only after I tried to fake an assistant message in `toPiAiMessage`.
3. **Vitest 4 changed `it()` signature.** Third positional arg is now `optionsOrTest`, not a number. Pass `{ timeout: ms }` as the second arg, or wrap in a helper that does so.
4. **pi-ai's openai-completions transport STILL requires a non-empty apiKey** for Ollama. Quirk discovered in Sprint 0 — `getApiKey("ollama")` returns the literal string `"ollama"` to satisfy this.
5. **`stream.result()` returns the final `AssistantMessage` but pi-ai also exposes `errorMessage` on it.** If a stream silently fails without emitting an `error` event, the only signal is `final.errorMessage`. The executor checks both paths.

---

## Gaps Worth Watching (all sprints)

These are systemic risks I want visible here rather than buried in the plan:

- **pi-ai is pre-1.0** (0.67.1 as of this writing). Breaking changes are likely. We depend on: `stream`, `Context` shape, `getModel('groq', …)`, `Model<'openai-completions'>` custom model shape, `AssistantMessageEventStream` event types. Any of these shifting would ripple through the Operation Executor.
- **Single-machine assumption.** Everything runs on localhost. No thought given to deploy, multi-user, or secret management beyond `.env.local`. Acceptable for the "local interface" phase of the project, but we should flag this if the product grows.
- **No structured logging yet.** `console.log` implicit. Operation Executor will need structured logging in Sprint 2 to make production issues debuggable.
- **pi-ai doesn't natively support Anthropic prompt caching via its unified interface in our code path** (we'd need provider-specific options). The implementation plan assumed cache_control would pass through — need to verify this survives the abstraction when we hit Sprint 6 batch ops.
- **No write locking on storage OR token tracker.** A single-user local app with one tab open is fine, but two concurrent SSE streams from the same session could race on both `addMessage` and `TokenTracker.record`. Sprint 4 (Session Manager) needs a per-session async mutex.
- **Path traversal in wireframe filenames** is not yet defended against. Must be sanitized in Sprint 8 when the wireframe-serving route lands.
- **Qwen thinking-mode flag is provider-version-dependent.** The `chat_template_kwargs.enable_thinking` flag works on qwen3 family models served by recent Ollama releases. If the user upgrades Ollama or switches model, the flag may be ignored silently and ops will become 30-40s slow per call without any error. Watch for this when tuning models.

---

## Current Environment Snapshot

- **OS:** Windows 11 Home (26200)
- **Node:** v22.20.0
- **npm:** 10.9.3
- **Next.js:** 16.2.3 (Turbopack)
- **Vitest:** 4.1.4
- **pi-ai:** `@mariozechner/pi-ai@0.67.1` (resolved via `^0.67.1` — minor breaking changes will surface as test failures)
- **Ollama:** Running at `localhost:11434`. Models present: `qwen3.5:4b`, `qwen3:4b`, `embeddinggemma:latest`
- **Active provider:** Ollama (`.env.local`)
- **Groq API key:** Not set
- **Test count:** 122 passing (118 unit + 4 live-Ollama integration)
  - Sprint 1: 64 (57 storage + 7 wireframe)
  - Sprint 2: 58 (36 parsers + 6 token-tracker + 4 two-ai + 8 executor-unit + 4 executor-integration)
